<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Fresh Market</title>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  <style>
    body {
      background-color: #f7f7f5;
      color: #2e2e2e;
      font-family: "Segoe UI", Arial, sans-serif;
      margin: 0;
      padding: 0;
    }

    /* STICKY NAVBAR */
    nav.navbar {
      background-color: #2d2d2d !important;
      position: sticky;
      top: 0;
      z-index: 9999;
    }

    nav .navbar-brand,
    nav .nav-link {
      color: #ffffff !important;
      font-weight: 600;
    }

    nav .nav-link:hover {
      background-color: #f2f2f2;
      color: #000 !important;
      border-radius: 5px;
      transition: 0.3s ease;
    }

    /* HERO - STATIC BANNER */
    .hero {
      position: relative;
      width: 100%;
      min-height: 65vh;
      display: flex;
      align-items: center;
      justify-content: center;
      text-align: center;
      padding: 40px 20px;
      overflow: hidden;
      border-bottom: 6px solid #d9d9d6;
      background: url('/images/mainfruit.png') center/cover no-repeat;
      color: #fff;
    }

    .hero-overlay {
      position: absolute;
      inset: 0;
      background: rgba(0,0,0,0.45);
      backdrop-filter: blur(1px);
      z-index: 1;
    }

    .hero-content {
      position: relative;
      z-index: 2;
      max-width: 900px;
      padding: 30px;
    }

    .hero-content h1 {
      font-size: 3.6rem;
      margin-bottom: 8px;
      font-weight: 800;
      letter-spacing: 1px;
    }

    .hero-content h3 {
      font-size: 1.4rem;
      margin-bottom: 14px;
      color: #f0f0f0;
      font-weight: 400;
    }

    .hero-content p {
      font-size: 1.05rem;
      margin-bottom: 20px;
      color: #eee;
    }

    .btn-shop {
      padding: 10px 22px;
      font-size: 1rem;
      border-radius: 6px;
      background-color: #f1f1f1;
      color: #2e2e2e;
      border: none;
      font-weight: 700;
      text-decoration: none;
    }

    .btn-shop:hover {
      background-color: #d9d9d9;
    }

    /* CAROUSEL (separate block) */
    .carousel-wrapper {
      padding: 40px 0;
      background: transparent;
    }

    .carousel .carousel-item img {
      height: 60vh;
      object-fit: cover;
    }

    .carousel-caption {
      background: rgba(0,0,0,0.4);
      border-radius: 8px;
      padding: 10px 14px;
    }

    /* FEATURED CARDS */
    .featured-row {
      padding: 50px 8%;
      display: flex;
      justify-content: center;
      gap: 24px;
      flex-wrap: wrap;
    }

    .featured-card {
      width: 300px;
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 6px 15px rgba(0,0,0,0.08);
      background-color: #fff;
      transition: 0.25s;
      text-decoration: none;
      color: inherit;
    }

    .featured-card:hover {
      transform: translateY(-6px);
      box-shadow: 0 10px 25px rgba(0,0,0,0.12);
    }

    .featured-card img {
      width: 100%;
      height: 200px;
      object-fit: cover;
    }

    .featured-card h5 {
      padding: 14px;
      margin: 0;
      font-size: 1.1rem;
      font-weight: 600;
      text-align: center;
    }

    @media (max-width: 768px) {
      .hero-content h1 { font-size: 2.2rem; }
      .carousel .carousel-item img { height: 40vh; }
      .featured-card { width: 100%; max-width: 420px; }
    }
  </style>
</head>

<body>
  <!-- NAVBAR -->
  <nav class="navbar navbar-expand-sm navbar-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Fresh Market</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item"><a class="nav-link" href="/register">Register</a></li>
          <li class="nav-item"><a class="nav-link" href="/login">Login</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- HERO (static single banner) -->
  <section class="hero" role="banner" aria-label="Hero banner">
    <div class="hero-overlay" aria-hidden="true"></div>

    <div class="hero-content">
      <h1>Fresh Market</h1>
      <h3>Daily Grown Ingredients Delivered Fresh</h3>
      <p>Your trusted local store for fresh fruits, vegetables and groceries.</p>
      <a class="btn-shop" href="/shopping">Shop Now</a>
    </div>
  </section>

  <!-- FEATURED CARDS -->
  <section class="featured-row" aria-label="Featured categories">
    <a href="/shopping" class="featured-card" aria-label="Premium Fresh Fruits">
      <img src="/images/fruits2.png" alt="Premium Fresh Fruits">
      <h5>Premium Fresh Fruits</h5>
    </a>

    <a href="/shopping" class="featured-card" aria-label="Organic Vegetables">
      <img src="/images/veg.png" alt="Organic Vegetables">
      <h5>Organic Vegetables</h5>
    </a>

    <a href="/shopping" class="featured-card" aria-label="Daily Essentials">
      <img src="/images/daily.png" alt="Daily Essentials">
      <h5>Daily Essentials</h5>
    </a>
  </section>
</body>
</html>
