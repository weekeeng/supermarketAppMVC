<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Fresh Market</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  <style>
    body {
      background-color: #f7f7f5; 
      color: #2e2e2e;
      font-family: "Segoe UI", Arial, sans-serif;
      margin: 0;
      padding: 0;
    }

    /* NAVBAR */
    nav.navbar {
      background-color: #2d2d2d !important;
    }

    nav .navbar-brand,
    nav .nav-link {
      color: #ffffff !important;
      font-weight: 600;
      letter-spacing: 0.3px;
    }

    nav .nav-link:hover {
      background-color: #f2f2f2;
      color: #000 !important;
      border-radius: 5px;
      transition: 0.3s ease;
    }

    /* HERO SECTION */
    .hero {
      position: relative;
      width: 100%;
      min-height: 75vh;
      display: flex;
      align-items: center;
      justify-content: center;
      color: #fff;
      text-align: center;
      padding: 40px 20px;
      overflow: hidden;
      border-bottom: 6px solid #d9d9d6;
    }

    .hero-overlay {
      position: absolute;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background: rgba(0, 0, 0, 0.45);
      backdrop-filter: blur(2px);
      z-index: 1;
    }

    .hero-content {
      position: relative;
      z-index: 2;
      max-width: 800px;
    }

    .hero-content h1 {
      font-size: 4rem;
      font-weight: 800;
      margin-bottom: 15px;
      letter-spacing: 1px;
    }

    .hero-content h3 {
      font-size: 1.6rem;
      margin-bottom: 10px;
      font-weight: 400;
      color: #f0f0f0;
    }

    .hero-content p {
      font-size: 1.2rem;
      margin-bottom: 25px;
    }

    .btn-shop {
      padding: 12px 28px;
      font-size: 1.2rem;
      border-radius: 6px;
      background-color: #f1f1f1;
      color: #2e2e2e;
      border: none;
      font-weight: bold;
      transition: 0.3s ease;
    }

    .btn-shop:hover {
      background-color: #d9d9d9;
    }

    /* FEATURED IMAGES */
    .featured-row {
      padding: 60px 10%;
      display: flex;
      justify-content: center;
      gap: 30px;
      flex-wrap: wrap;
    }

    .featured-card {
      width: 300px;
      border-radius: 12px;
      overflow: hidden;
      box-shadow: 0 6px 15px rgba(0, 0, 0, 0.1);
      background-color: #ffffff;
      transition: 0.3s;
    }

    .featured-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.15);
    }

    .featured-card img {
      width: 100%;
      height: 220px;
      object-fit: cover;
    }

    .featured-card h5 {
      padding: 15px;
      margin: 0;
      font-size: 1.2rem;
      font-weight: 600;
      text-align: center;
    }
  </style>
</head>

<body>

  <!-- NAVBAR -->
  <nav class="navbar navbar-expand-sm navbar-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Fresh Market</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item"><a class="nav-link" href="/register">Register</a></li>
          <li class="nav-item"><a class="nav-link" href="/login">Login</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- HERO SECTION WITH CAROUSEL -->
  <section class="hero">
    <!-- Carousel -->
    <div id="heroCarousel" class="carousel slide carousel-fade" data-bs-ride="carousel">
      <div class="carousel-inner">
        <div class="carousel-item active">
          <img src="/images/mainfruit.png" class="d-block w-100" alt="Fresh Market 1" style="height:75vh; object-fit:cover;">
        </div>
        <div class="carousel-item">
          <img src="/images/hero-bg2.jpg" class="d-block w-100" alt="Fresh Market 2" style="height:75vh; object-fit:cover;">
        </div>
        <div class="carousel-item">
          <img src="/images/hero-bg3.jpg" class="d-block w-100" alt="Fresh Market 3" style="height:75vh; object-fit:cover;">
        </div>
      </div>

      <!-- Optional controls -->
      <button class="carousel-control-prev" type="button" data-bs-target="#heroCarousel" data-bs-slide="prev">
        <span class="carousel-control-prev-icon"></span>
        <span class="visually-hidden">Previous</span>
      </button>
      <button class="carousel-control-next" type="button" data-bs-target="#heroCarousel" data-bs-slide="next">
        <span class="carousel-control-next-icon"></span>
        <span class="visually-hidden">Next</span>
      </button>
    </div>

    <!-- Overlay -->
    <div class="hero-overlay"></div>

    <!-- Hero Text -->
    <div class="hero-content">
      <h1>Fresh Market</h1>
      <h3>Daily Grown Ingredients Delivered Fresh</h3>
      <p>Your trusted local store for fresh fruits, vegetables, and groceries.</p>
      <li class="nav-item"><a class="btn-shop" href="/login">Shop Now</a></li>
    </div>
  </section>

  <!-- FEATURED ITEMS -->
  <div class="featured-row">
    <div class="featured-card">
      <img src="/images/apples.png" alt="Fresh Fruits">
      <h5>Premium Fresh Fruits</h5>
    </div>
    <div class="featured-card">
      <img src="/images/broccoli.png" alt="Healthy Vegetables">
      <h5>Organic Vegetables</h5>
    </div>
    <div class="featured-card">
      <img src="/images/tomatoes.png" alt="Groceries">
      <h5>Daily Essentials</h5>
    </div>
  </div>

</body>
</html>
