<!DOCTYPE html>
<html>
<head>
    <title>Order Confirmation</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

<div class="container py-5">
    <h2>Order Confirmed</h2>
    <p class="text-success">Thank you, <strong><%= order.fullName %></strong>! Your order has been placed.</p>

    <div class="card p-4 mb-4">
        <h5>Order Details</h5>
        <p><strong>Order ID:</strong> <%= order.id %></p>
        <p><strong>Placed At:</strong> <%= order.createdAt.toLocaleString() %></p>
        <p><strong>Payment Method:</strong> <%= order.paymentMethod %></p>
        <p><strong>Delivery Address:</strong> <%= order.address %></p>
        <p><strong>Contact:</strong> <%= order.contact %></p>
    </div>

    <div class="card p-4 mb-4">
        <h5>Items</h5>
        <ul class="list-group">
            <% order.cart.forEach(item => { %>
                <li class="list-group-item d-flex justify-content-between align-items-center">
                    <div>
                        <strong><%= item.productName || item.name || 'Item' %></strong>
                        <div class="small text-muted">Qty: <%= item.quantity %></div>
                    </div>
                    <div>$<%= (parseFloat(item.price) * parseInt(item.quantity)).toFixed(2) %></div>
                </li>
            <% }) %>
        </ul>
        <div class="mt-3 text-end fw-bold">Total: $<%= order.total %></div>
    </div>

    <a href="/shopping" class="btn btn-primary">Continue Shopping</a>
</div>

</body>
</html>
