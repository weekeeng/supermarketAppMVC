<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Product Details | SunnySide Market</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

  <style>
    body {
      background-color: #fff; /* white */
      font-family: Arial, sans-serif;
      color: #000;
    }

    .navbar {
      background-color: #000 !important; /* black */
    }

    .navbar .nav-link {
      color: #fff !important;
      font-weight: bold;
    }

    .navbar .nav-link:hover {
      background-color: #fff; /* white */
      color: #000 !important;
      border-radius: 5px;
    }

    .product-card {
      background-color: #fff; /* white */
      border-radius: 15px;
      padding: 20px;
      max-width: 500px;
      margin: 40px auto;
      box-shadow: 0 0 15px rgba(0,0,0,0.08);
      color: #000;
    }

    .product-card img {
      max-width: 100%;
      border-radius: 10px;
      margin-bottom: 20px;
    }

    .product-card h2 {
      color: #000; /* black */
      margin-bottom: 20px;
    }

    .product-card p {
      font-size: 1.1rem;
      color: #000;
      margin-bottom: 15px;
    }

    .btn-back {
      background-color: #000; /* black */
      color: #fff;
      border: none;
      font-weight: bold;
      margin-top: 20px;
    }

    .btn-back:hover {
      background-color: #fff; /* white */
      color: #000;
      border: 1px solid #000;
    }
  </style>
</head>
<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-sm navbar-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="#">Fresh Market</a>
      <div class="collapse navbar-collapse" id="collapsibleNavbar">
        <ul class="navbar-nav ms-auto">
          <% if (user.role === "admin") { %>
            <li class="nav-item"><a class="nav-link" href="/inventory">Inventory</a></li>
            <li class="nav-item"><a class="nav-link" href="/addProduct">Add Product</a></li>
          <% } %>
          <li class="nav-item"><a class="nav-link" href="/logout">Logout</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Product Details -->
  <div class="product-card text-center">
    <h2><b>Product Details</b></h2>

    <% if (product) { %>
      <img src="/images/<%= product.image %>" alt="<%= product.productName %>">
      <p><strong>Product Name:</strong> <%= product.productName %></p>
      <p><strong>Category:</strong> <%= product.category %></p>
      <p><strong>Quantity:</strong> <%= product.quantity %></p>
      <p><strong>Price:</strong> $<%= product.price.toFixed(2) %></p>
    <% } else { %>
      <p>No product found.</p>
    <% } %>

    <% if (user.role === "admin") { %>
      <a href="/inventory" class="btn btn-back">Back</a>
    <% } else { %>
      <a href="/shopping" class="btn btn-back">Back</a>
    <% } %>
  </div>
</body>
</html>
